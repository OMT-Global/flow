# Hermes Lane Contract

Hermes is the macOS/platform-native and special execution lane.

Responsibilities:

- run macOS-native checks;
- validate platform behavior unavailable on Linux/NAS workers;
- provide overflow or recovery execution when explicitly assigned;
- keep Hermes GitHub identity separate from JT's personal identity.

Hermes should not inherit generic backlog unless platform-native value exists.
